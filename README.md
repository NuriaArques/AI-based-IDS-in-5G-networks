# AI-based-IDS-in-5G-networks




## Install dependencies

To install all the necessary Python packages listed in the `requirements.txt` file, run the following command:

```
pip install -r requirements.txt
```
